/** @format */

import React from "react";

const SiteSettings = () => {
  return (
    <div style={{ padding: 32, textAlign: "center", fontSize: 32 }}>
      Admin SiteSettings Page
    </div>
  );
};

export default SiteSettings;
