/** @format */

import { registerPlayer } from "../service/gameChoose.service";

// Example usage: If you have a form, call registerPlayer on submit
// export const handleSubmit = async (formData: any) => {
//   try {
//     await registerPlayer(formData);
//     // handle success (navigate, show message, etc.)
//   } catch (err) {
//     alert("Registration failed. Try again.");
//   }
// };
